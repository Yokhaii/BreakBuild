--[=[
	CloseButton Configuration
]=]

local Config = {
	-- Default image
	Image = "rbxassetid://6031094678", -- Replace with your close button asset

	-- Default colors
	DefaultColor = Color3.fromRGB(0, 0, 0),
	HoverColor = Color3.fromRGB(20, 20, 20),

	-- Animation config
	HoverScale = 1.1,
	ClickScale = 0.8,
	SpringTension = 100,
	SpringFriction = 10,
}

return Config
