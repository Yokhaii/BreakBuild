--[=[
	StudBackground Configuration
]=]

return {
	-- Default image (stud pattern)
	Image = "rbxassetid://6927295847",

	-- Colors
	BackgroundColor = Color3.fromRGB(40, 40, 50),
	ImageColor = Color3.fromRGB(255, 255, 255),
	ImageTransparency = 0.75,

	-- Reference: the screen where BaseTileSize looks correct
	ReferenceWidth = 1920,

	-- Tile size at reference resolution (1920 width)
	-- Adjust this to change how many tiles you see
	BaseTileSize = 100,
}
